# Experiment
Primary metric: time from abnormal spend onset to accountable-owner notification.
Compare a traditional budget-threshold baseline with CloudSpend Sentinel.
Metrics: notification latency, precision, recall, F1, false-positive/false-negative rate, root-cause accuracy and evidence completeness.
Run: `python -m pytest -q`.
The dashboard benchmark is a prototype replay benchmark and should be replaced/updated with your final measured run before submission.
